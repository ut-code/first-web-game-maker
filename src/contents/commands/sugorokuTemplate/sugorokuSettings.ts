const sugorokuSettings = `
    <script>
      // ゲームの定数と変数
      const BOARD_SIZE = 25;
      const CELL_SIZE = 100;
      const PLAYER_SIZE = 20;
      const positions = new Array(1, 1, 1, 1);
      userNum = 0;
    </script>
`;
export default sugorokuSettings;
