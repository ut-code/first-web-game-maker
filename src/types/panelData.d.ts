/**
 * the type of panel data
 * @typedef {Object} PanelData
 * @property {string} title - the title of panel
 * @property {string} path - the path of panel
 */
export type PanelData = {
  title: string;
  path: string;
};
